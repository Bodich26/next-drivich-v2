import { LoadingPage } from "@/widgets/loading";

export default function Loading() {
  return (
    <LoadingPage
      title="Loading Dashboard"
      subtitle="Preparing your admin panel..."
    />
  );
}
